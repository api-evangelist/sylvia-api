# Sylvia API

> Read-only Reddit data API. Structured JSON access to posts, comments, subreddits, user profiles, search, and live streams — no OAuth, no KYC.

Sylvia API lets you fetch any public Reddit data via simple HTTP requests. Sign up, get an API key, and start making requests in seconds. Also available as an MCP server for AI agents.

## Base URL

```
https://api.sylvia-api.com/v1/reddit
```

## Authentication

All requests require an API key via the `X-API-KEY` header:

```bash
curl -H "X-API-KEY: syl_your_key_here" \
  "https://api.sylvia-api.com/v1/reddit/r/all/hot?limit=25"
```

Sign up at https://sylvia-api.com to get your key.

## MCP Server

Model Context Protocol endpoint for direct agent tool-calling: `https://api.sylvia-api.com/mcp`

## Pricing

- **Standard requests**: $0.0005 each
- **Live streams**: $0.05 each (`/comments/live`, `/r/{sub}/comments/live`)
- **Search**: $0.05 each
- **Free tier**: $0.50 credit on signup (~1,000 requests)
- **Tiers**: Free, Strela ($50+/30d), Svetka ($200+/30d), Enterprise ($500+/30d)
- Payments: crypto only (USDT, BTC, LTC, BNB via NowPayments)
- Failed requests (4xx, 5xx) are never charged

## Rate Limits

| Tier | Requests/min | Burst |
|------|--------------|-------|
| Free | 480 | 15 |
| Strela | 1,200 | 40 |
| Svetka | 2,400 | 80 |
| Enterprise | 3,600 | 100 |

Headers: `X-RateLimit-Tier`, `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`, `Retry-After`.

## Endpoints

### Subreddits

| Endpoint | Description |
|----------|-------------|
| `GET /r/{subreddit}/about` | Subreddit info (members, description, icon) |
| `GET /r/{subreddit}/about/rules` | Subreddit rules |
| `GET /r/{subreddit}/{sort}` | Posts — hot, new, top, rising, controversial |
| `GET /r/{subreddit}/comments/live` | Real-time comment stream |
| `GET /r/{subreddit}/sticky` | Sticky/pinned posts |
| `GET /r/{subreddit}/wiki/pages` | List wiki pages |
| `GET /r/{subreddit}/wiki/{page}` | Wiki page content |

### Users

| Endpoint | Description |
|----------|-------------|
| `GET /u/{username}/about` | Profile metadata |
| `GET /u/{username}/submitted` | User's posts |
| `GET /u/{username}/comments` | User's comments |
| `GET /u/{username}/overview` | Combined posts + comments |

### Posts & Comments

| Endpoint | Description |
|----------|-------------|
| `GET /submission?url={reddit_url}` | Fetch post by URL |
| `GET /comment?url={reddit_url}` | Fetch comment by URL |
| `GET /submission/{id}/full` | Full thread with all nested comments |
| `GET /duplicates/{id}` | Crosspost duplicates |
| `GET /domain/{domain}` | Posts linking to a domain |
| `GET /morechildren` | Load more comments |
| `GET /by_id` | Fetch by fullname IDs (e.g. `t3_abc,t3_def`) |

### Discovery

| Endpoint | Description |
|----------|-------------|
| `GET /subreddits/{category}` | Browse subreddits — popular, default, new, premium |
| `GET /subreddits/search` | Search subreddits by name or description |
| `GET /subreddit_autocomplete` | Autocomplete subreddit names |

### Search

| Endpoint | Description |
|----------|-------------|
| `GET /search` | Search Reddit posts globally |

### Global Feeds

| Endpoint | Description |
|----------|-------------|
| `GET /r/all/{sort}` | r/all feed — hot, new, top, rising, controversial |
| `GET /comments/live` | Global comment firehose |

### Account

| Endpoint | Description |
|----------|-------------|
| `GET /auth/me` | Account profile (email, tier, balance, status) |
| `GET /keys` | List API keys (masked prefixes) |
| `POST /keys` | Create a key (full key shown once) |
| `DELETE /keys/{id}` | Revoke a key |
| `GET /templates` / `POST /templates` | List or create custom templates |
| `DELETE /templates/{name}` | Delete a custom template |
| `GET /auth/formats` | List formats and availability on your tier |
| `POST /auth/default-format` | Set the account default format |
| `GET /auth/recent-requests` | Recent request history |
| `GET /auth/usage-history` | Per-day usage analytics |
| `POST /createPayment` | Create a crypto deposit payment link |

Account endpoints require the account token (`x-sylvia-auth: SV_...` or `Authorization: Bearer SV_...`).

## Response Format

```json
{
  "success": true,
  "data": {
    "posts": [],
    "after": "t3_nextpage"
  },
  "request_id": "uuid"
}
```

## Machine-readable artifacts

- OpenAPI: https://sylvia-api.com/openapi.json
- APIs.json index: https://sylvia-api.com/apis.json
- Plans: https://sylvia-api.com/plans.json
- Rate limits: https://sylvia-api.com/rate-limits.json
- FinOps: https://sylvia-api.com/finops.json
- Agent card: https://sylvia-api.com/.well-known/agent-card.json
- API catalog: https://sylvia-api.com/.well-known/api-catalog
- Security policy: https://sylvia-api.com/security/
- Status: https://sylvia-api.instatus.com/
- Changelog: https://sylvia-api.com/changelog/

## Links

- Website: https://sylvia-api.com
- Dashboard: https://sylvia-api.com
- API docs: https://sylvia-api.com/docs/
